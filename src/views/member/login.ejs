<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!--
        matchmember == memberDAO
        -->
<script>
    const match = () => {
        console.log("match 가동")
        let userid = document.getElementById(id)
        let username = document.getElementById(name)
        let num=0;

        for(let i=0 ; i < matchmember.length ; i++){
            if (name == username[i]){
                if (id == userid[i]){
                    num = 1;
                }
            }
        }
        if(num == 1){
            <a href = "/list">aa</a> 
            /*스크립트 안에서 html쓸수 없음
            방법은 있는데 찾아봐야함. inneerHTMl, append(Child) 등등..
            */
        }else{
            alert("회원정보가 맞지 않습니다");
            <a href = "/login"></a>
        }
    }
     
</script>
    <div>
        <form action="/login_check" method="get">
            <input type="text" name="id" placeholder="input id"><br>
            <input type="text" name="name" placeholder="input name"><br>
            <button type="submit">로그인</button>
        </form>
    </div>
    
</body>
</html>